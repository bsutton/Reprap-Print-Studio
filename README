Compiling
-------------------
Run
cd convertor
mvn install

The compiled classes will be in the convertor/target/classes directory.

Usage
-------------------
$ ./run.sh input_type input_file output_type output_file

Where input_type and output_type is one of:
"stl", "stlb", or "obj"

Known Issues
-------------------
The OBJ parser does not currently support materials or textures.
